<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bizfit.bizfit">

    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- TODO Atte, was ist das? -->
    AccountManager.getAccountsByType: android.permission.GET_ACCOUNTS
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name=".MyAlarmService" />

        <!-- Splash screen -->
        <activity
            android:name=".activities.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="user"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!--
                Uncomment for splash screen
                <category android:name="android.intent.category.LAUNCHER" />
                -->
            </intent-filter>
        </activity>

        <!-- Home view -->
        <activity
            android:name=".activities.MainPage"
            android:label="@string/app_name"
            android:screenOrientation="user"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <!-- Comment for splash screen -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <!-- Settings -->
        <activity
            android:name=".activities.Settings"
            android:label="@string/action_settings"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <!-- Start a new goal -->
        <activity
            android:name=".activities.AddTracker"
            android:label="@string/title_new_goal"
            android:screenOrientation="user"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan|stateVisible"></activity>

        <!-- Single tracker info -->
        <activity
            android:name=".activities.ViewTracker"
            android:label=""
            android:screenOrientation="user"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <!-- Store page -->
        <activity android:name=".activities.CoachPage"></activity>

        <!-- Displays search results -->
        <activity android:name=".activities.Search">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>
    </application>

</manifest>
